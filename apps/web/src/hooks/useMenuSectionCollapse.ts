/** Kept for MenuTabs import compatibility; collapse/clipPath removed to stop menu jitter. */
export function useMenuSectionCollapse() {
  // no-op: sticky tabs + product clipping caused visible shake and orphan price pills
}

export function resetMenuSection(_sectionId: string) {
  // no-op
}
